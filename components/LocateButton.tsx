import React from "react";
import { Button } from "react-native";

const LocateButton = () => {
    return (
        <Button title="Locate Schools" onPress={() => alert("pressed")}/>
    )
}

export default LocateButton;